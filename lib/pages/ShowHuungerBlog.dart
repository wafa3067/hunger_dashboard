import 'package:flutter/material.dart';

class HungerBlog extends StatefulWidget {

  const HungerBlog({Key? key}) : super(key: key);

  @override
  State<HungerBlog> createState() => _HungerBlogState();

}

class _HungerBlogState extends State<HungerBlog> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body:ListView(),);
  }
}
